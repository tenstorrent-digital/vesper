name: Vesper Tests

on:
  pull_request:
    paths:
      - "packages/vesper/**"
  push:
    paths:
      - "packages/vesper/**"

  # Empty `workflow_dispatch` allows this workflow to be run manually from the Actions tab:
  workflow_dispatch:

# For pull_request events, github.ref is refs/pull/N/merge, but github.head_ref is the branch name
# For push events, github.head_ref is empty, so it falls back to github.ref
# push and pull_request events resolve to the same concurrency group this way
concurrency:
  group: vesper-test-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: "yarn"

      - name: Install Dependencies
        run: yarn install --frozen-lockfile

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps chromium

      - name: Run Tests
        run: yarn test:vesper

      - name: Upload Artifacts
        if: ${{ !cancelled() && failure() }}
        uses: actions/upload-artifact@v6
        with:
          name: test-results
          path: packages/vesper/test-results/
          retention-days: 7
