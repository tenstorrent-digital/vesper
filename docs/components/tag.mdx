---
title: Tag
description: A small label component for categorizing or annotating content.
---

# Tag

A small label component for categorizing or annotating content. Supports a wide range of color variants, three sizes, and an optional leading icon.

<div style={{ display: "flex", gap: "8px" }}>
  <Tag>Default</Tag>
  <Tag variant="accent-bold">Accent</Tag>
  <Tag variant="success-bold">Success</Tag>
  <Tag variant="danger-bold">Danger</Tag>
</div>

## Options

| Prop       | Type                                                                                                                                                                                                          | Description                                                                | Default     |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ----------- |
| `size`     | `"sm" \| "md" \| "lg"`                                                                                                                                                                                        | The size of the tag. Affects padding and font size.                        | `"lg"`      |
| `variant`  | `"default" \| "contrast" \| "accent-bold" \| "accent-subtle" \| "danger-bold" \| "danger-subtle" \| "success-bold" \| "success-subtle" \| "info-bold" \| "info-subtle" \| "warning-bold" \| "warning-subtle"` | The color variant of the tag.                                              | `"default"` |
| `disabled` | `boolean`                                                                                                                                                                                                     | When `true`, renders the tag in a disabled state and prevents interaction. | `false`     |
| `icon`     | `ReactNode`                                                                                                                                                                                                   | An optional icon element rendered to the left of the tag text.             | —           |
| `as`       | `ElementType`                                                                                                                                                                                                 | The root element type for polymorphic rendering.                           | `"div"`     |
| `children` | `ReactNode`                                                                                                                                                                                                   | The text content of the tag.                                               | —           |

## Examples

```tsx
import { Tag } from "@tenstorrent/vesper/tag";

// Basic usage
<Tag>Label</Tag>;
```

```tsx
// Color variants
<Tag variant="accent-bold">Feature</Tag>
<Tag variant="success-subtle">Completed</Tag>
<Tag variant="danger-bold">Critical</Tag>
<Tag variant="warning-subtle">Review</Tag>
<Tag variant="info-bold">Note</Tag>
```

```tsx
// Sizes
<Tag size="sm">Small</Tag>
<Tag size="md">Medium</Tag>
<Tag size="lg">Large</Tag>
```

```tsx
// With icon
import { Bolt } from "@tenstorrent/vesper/icons";

<Tag icon={<Bolt />} variant="accent-bold">
  Performance
</Tag>;
```

```tsx
// Rendered as a link
<Tag as="a" href="/category/design" variant="contrast">
  Design
</Tag>
```
