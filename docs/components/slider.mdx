---
title: Slider
description: A single-thumb slider for selecting a numeric value within a range.
---

# Slider

A single-thumb slider for selecting a numeric value within a range. It is built on top of [the `Range` component](./range.mdx), and exposes the same track, ticks, labels, messages, and variants through a single-value API.

<Slider label="Volume" thumbAriaLabel="Volume" defaultValue={50} />

## Options

| Prop             | Type                                             | Description                                                                                       | Default     |
| ---------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------- | ----------- |
| `thumbAriaLabel` | `string`                                         | The accessible `aria-label` for the thumb. Required.                                              | —           |
| `value`          | `number`                                         | The value of the thumb (controlled mode).                                                         | —           |
| `defaultValue`   | `number`                                         | The initial thumb value (uncontrolled mode).                                                      | `min`       |
| `onValueChange`  | `(value: number) => void`                        | Callback fired as the thumb value changes during interaction.                                     | —           |
| `onValueCommit`  | `(value: number) => void`                        | Callback fired when the thumb interaction is completed, eg. on pointer up.                        | —           |
| `min`            | `number`                                         | The minimum allowed value.                                                                        | `0`         |
| `max`            | `number`                                         | The maximum allowed value.                                                                        | `100`       |
| `step`           | `number`                                         | The stepping interval between selectable values.                                                  | `1`         |
| `showTicks`      | `boolean`                                        | When `true`, renders tick marks along the track at each step interval.                            | `false`     |
| `showValueLabel` | `boolean`                                        | When `true`, displays a value label above the thumb.                                              | `false`     |
| `valueLabel`     | `string`                                         | A custom display label for the thumb. When unset, it falls back to the thumb's current value.     | —           |
| `variant`        | `"default" \| "warning" \| "success" \| "error"` | The visual variant, which determines the colour scheme and icon of the message.                   | `"default"` |
| `label`          | `string`                                         | An optional label displayed above the track, and used as the accessible name of the slider group. | —           |
| `message`        | `string`                                         | An optional message displayed below the track, paired with a variant-specific icon.               | —           |
| `disabled`       | `boolean`                                        | When `true`, prevents interaction.                                                                | `false`     |
| `name`           | `string`                                         | The form field name submitted with form data.                                                     | —           |
| `form`           | `string`                                         | Associates the slider with a `<form>` element by its `id`.                                        | —           |

Any other props are forwarded to the wrapping `<div>` element.

If you need to select a span of values rather than a single one, reach for [the `Range` component](./range.mdx) instead: it renders the same track, but with a thumb per value.

## Examples

### Uncontrolled vs controlled

The simplest way to use the `Slider` component is to render it in an uncontrolled fashion, where the component keeps track of its own value. Pass `defaultValue` if the thumb should start somewhere other than `min`:

<Slider label="Volume" thumbAriaLabel="Volume" defaultValue={30} />

```tsx
import { Slider } from "@tenstorrent/vesper/slider";

<Slider label="Volume" defaultValue={30} thumbAriaLabel="Volume" />;
```

If you need to control the value, use the `value` and `onValueChange` props. `onValueChange` fires continuously as the thumb is dragged:

<SliderDemo kind="controlled" />

```tsx
import { useState } from "react"
import { Slider } from "@tenstorrent/vesper/slider"

const [volume, setVolume] = useState(75)

<Slider
  label="Volume"
  value={volume}
  onValueChange={setVolume}
  thumbAriaLabel="Volume"
/>
```

Use `onValueCommit` when you only care about the final value of an interaction, eg. to avoid firing a request on every pointer move:

```tsx
import { Slider } from "@tenstorrent/vesper/slider";

<Slider
  label="Volume"
  defaultValue={50}
  onValueCommit={(volume) => savePreference({ volume })}
  thumbAriaLabel="Volume"
/>;
```

### Bounds and stepping

`min`, `max`, and `step` describe the values the thumb can take, defaulting to `0`, `100`, and `1`. A `step` of `100` on a range of `0-1000` restricts the thumb to multiples of 100:

<Slider
  label="Budget"
  min={0}
  max={1000}
  step={100}
  defaultValue={500}
  showValueLabel
  thumbAriaLabel="Budget"
/>

```tsx
import { Slider } from "@tenstorrent/vesper/slider";

<Slider
  label="Budget"
  min={0}
  max={1000}
  step={100}
  defaultValue={500}
  showValueLabel
  thumbAriaLabel="Budget"
/>;
```

The `step` also determines how far the thumb moves per arrow key press when it is focused.

> [!NOTE]
>
> When neither `value` nor `defaultValue` is provided, the thumb starts at `min` rather than at zero, so a slider with a `min` of `10` starts at `10`.

### Showing ticks

Pass `showTicks` to mark each step along the track. Ticks are rendered between `min` and `max`, so a range of `0-10` with a `step` of `1` renders nine of them:

<Slider
  label="Rating"
  min={0}
  max={10}
  step={1}
  showTicks
  defaultValue={3}
  thumbAriaLabel="Rating"
/>

```tsx
import { Slider } from "@tenstorrent/vesper/slider";

<Slider
  label="Rating"
  min={0}
  max={10}
  step={1}
  showTicks
  defaultValue={3}
  thumbAriaLabel="Rating"
/>;
```

> [!NOTE]
>
> Ticks are drawn per step, so a fine `step` on a wide range produces a dense, noisy track. Ticks work best when the range has a handful of stops, eg. a range of `0-100` with a `step` of `10`.

### Value label

Pass `showValueLabel` to render the thumb's current value above it as you drag:

<Slider
  label="Volume"
  showValueLabel
  defaultValue={40}
  thumbAriaLabel="Volume"
/>

```tsx
import { Slider } from "@tenstorrent/vesper/slider";

<Slider
  label="Volume"
  showValueLabel
  defaultValue={40}
  thumbAriaLabel="Volume"
/>;
```

Use `valueLabel` to display something other than the raw value, such as a formatted or unit-suffixed one. It is only rendered when `showValueLabel` is also passed:

<SliderDemo kind="custom-thumb-labels" />

```tsx
import { useState } from "react"
import { Slider } from "@tenstorrent/vesper/slider"

const [volume, setVolume] = useState(40)

<Slider
  label="Volume"
  showValueLabel
  value={volume}
  onValueChange={setVolume}
  valueLabel={`${volume}%`}
  thumbAriaLabel="Volume"
/>
```

> [!IMPORTANT]
>
> Value label is presentational, and is not announced by assistive technology.
>
> Screen readers announce the underlying numeric value of a thumb instead. If a label carries meaning that the number alone doesn't, such as a currency or a unit, include it in the `thumbAriaLabel` too.

### Message

You can render an additional line of text under the track via the `message` prop. Combine it with the `variant` prop to indicate the tone of the message to the user.

<Slider
  label="Volume"
  message="Applies to notification sounds only."
  defaultValue={50}
  thumbAriaLabel="Volume"
/>

```tsx
import { Slider } from "@tenstorrent/vesper/slider";

<Slider
  label="Volume"
  message="Applies to notification sounds only."
  defaultValue={50}
  thumbAriaLabel="Volume"
/>;
```

The message is linked to the slider via `aria-describedby`, so it is announced along with the field by assistive technology.

### Variants

The `Slider` component can be rendered in one of four variants: `default`, `warning`, `success`, or `error`. The variant determines the colour scheme of the message, as well as the icon paired with it.

#### Default variant

A `Slider` renders with the `default` variant when no `variant` prop is specified. Its `message` is best suited for neutral hints:

<Slider
  label="Volume"
  message="You can change this later in your settings."
  defaultValue={50}
  thumbAriaLabel="Volume"
/>

```tsx
import { Slider } from "@tenstorrent/vesper/slider";

<Slider
  label="Volume"
  message="You can change this later in your settings."
  defaultValue={50}
  thumbAriaLabel="Volume"
/>;
```

#### Warning variant

Use the `warning` variant to flag a value that needs attention, but that doesn't prevent the form from being submitted:

<Slider
  variant="warning"
  label="Volume"
  message="Sustained volumes above 85 can damage hearing."
  defaultValue={95}
  showValueLabel
  thumbAriaLabel="Volume"
/>

```tsx
import { useState } from "react";
import { Slider } from "@tenstorrent/vesper/slider";

const [volume, setVolume] = useState(95);

<Slider
  variant="warning"
  label="Volume"
  message="Sustained volumes above 85 can damage hearing."
  value={volume}
  onValueChange={setVolume}
  showValueLabel
  thumbAriaLabel="Volume"
/>;
```

#### Success variant

The `success` variant is best suited for confirming that a value has been validated as expected:

<Slider
  variant="success"
  label="Workers"
  min={1}
  max={16}
  message="This fits comfortably within your quota."
  defaultValue={4}
  showValueLabel
  thumbAriaLabel="Workers"
/>

```tsx
import { useState } from "react";
import { Slider } from "@tenstorrent/vesper/slider";

const [workers, setWorkers] = useState(4);

<Slider
  variant="success"
  label="Workers"
  min={1}
  max={16}
  message="This fits comfortably within your quota."
  value={workers}
  onValueChange={setWorkers}
  showValueLabel
  thumbAriaLabel="Workers"
/>;
```

#### Error variant

The `error` variant highlights an invalid value that must be corrected before the form can be submitted:

<Slider
  variant="error"
  label="Workers"
  min={1}
  max={16}
  message="Your plan allows a maximum of 8 workers."
  defaultValue={12}
  showValueLabel
  aria-invalid
  thumbAriaLabel="Workers"
/>

```tsx
import { useState } from "react";
import { Slider } from "@tenstorrent/vesper/slider";

const [workers, setWorkers] = useState(12);

<Slider
  variant="error"
  label="Workers"
  min={1}
  max={16}
  message="Your plan allows a maximum of 8 workers."
  value={workers}
  onValueChange={setWorkers}
  showValueLabel
  aria-invalid
  thumbAriaLabel="Workers"
/>;
```

The `variant` only affects the appearance of the message. Pass `aria-invalid` alongside the `error` variant so that assistive technology is told the value is invalid too.

### Labels and accessible names

> [!IMPORTANT]
>
> `thumbAriaLabel` is required, and names the thumb the user actually interacts with. Without it, the thumb is announced with no indication of what it controls.

The optional `label` prop names the slider as a whole. When provided, it is rendered above the track and associated with the thumb, so clicking it focuses the thumb:

<Slider label="Volume" defaultValue={50} thumbAriaLabel="Volume" />

```tsx
import { Slider } from "@tenstorrent/vesper/slider";

<Slider label="Volume" defaultValue={50} thumbAriaLabel="Volume" />;
```

If you don't want a visible label, pass an `aria-label` instead to name the slider group:

<Slider aria-label="Volume" defaultValue={50} thumbAriaLabel="Volume" />

```tsx
import { Slider } from "@tenstorrent/vesper/slider";

<Slider aria-label="Volume" defaultValue={50} thumbAriaLabel="Volume" />;
```

### Keyboard interaction

The thumb is a native `<input type="range">`, so a `Slider` is fully keyboard operable once it is focused:

| Key                       | Behaviour                                      |
| ------------------------- | ---------------------------------------------- |
| `ArrowRight` / `ArrowUp`  | Increases the value by `step`.                 |
| `ArrowLeft` / `ArrowDown` | Decreases the value by `step`.                 |
| `Shift` + any arrow key   | Moves the thumb by 10 units instead of `step`. |
| `PageUp` / `PageDown`     | Increases / decreases the value by 10 units.   |
| `Home`                    | Moves the thumb to `min`.                      |
| `End`                     | Moves the thumb to `max`.                      |

### Disabling the slider

Pass `disabled` to prevent all interaction. A disabled `Slider` still displays its current value, but its thumb is skipped in the tab order and excluded from form data:

<Slider disabled label="Volume" defaultValue={50} thumbAriaLabel="Volume" />

```tsx
import { Slider } from "@tenstorrent/vesper/slider";

<Slider disabled label="Volume" defaultValue={50} thumbAriaLabel="Volume" />;
```

### Usage in forms

The thumb renders a native `<input type="range">`, so passing a `name` includes its value in form submissions. Use the `form` prop to associate the field with a `<form>` rendered elsewhere on the page:

<SliderDemo kind="form" />

```tsx
import { Button } from "@tenstorrent/vesper/button";
import { Slider } from "@tenstorrent/vesper/slider";

<form action="/api/preferences" method="post">
  <Slider
    name="volume"
    label="Volume"
    defaultValue={50}
    thumbAriaLabel="Volume"
  />
  <Button type="submit">Submit</Button>
</form>;
```

A range input always has a value, so a `Slider` submits its current value even if the user never touches it. The form above submits `volume=50` on an untouched slider.
