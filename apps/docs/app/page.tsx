import { Tenstorrent } from "@repo/react/icons";
import { colors } from "@repo/tokens";

export default function Page() {
  return (
    <Tenstorrent
      style={{ color: colors.light["teal-500"] }}
      width={48}
      height={48}
    />
  );
}
