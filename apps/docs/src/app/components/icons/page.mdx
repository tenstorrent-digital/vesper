import {
  Add,
  Gear,
  Search,
  Info,
  Globe,
  Tenstorrent,
} from "@repo/vesper/icons";

import { STORYBOOK_URL } from "@/lib/constants";

# Icons

A library of SVG icon components used throughout Vesper. Each icon renders as an inline `<svg>` element with `currentColor` fill, making it easy to style via CSS. An `Icon` component is also available for rendering icons dynamically by name. [View in Storybook]({`${STORYBOOK_URL}/icons`})

<div
  style={{
    display: "flex",
    gap: "16px",
    alignItems: "center",
    fontSize: "24px",
  }}
>
  <Add />
  <Gear />
  <Search />
  <Info />
  <Globe />
  <Tenstorrent />
</div>

## Options

Each individual icon component (e.g., `Add`, `Gear`, `Search`) accepts all standard `SVG` element props (`ComponentProps<"svg">`).

The dynamic `Icon` component accepts:

| Prop   | Type       | Description                                                                             | Default |
| ------ | ---------- | --------------------------------------------------------------------------------------- | ------- |
| `kind` | `IconKind` | The name of the icon to render. See the full list of available icon names in Storybook. | —       |

## Examples

```tsx
import { Add, Gear, Search } from "@repo/vesper/icons";

// Individual icon imports (tree-shakeable)
<Add />
<Gear />
<Search />
```

```tsx
// Sizing via CSS
<Gear style={{ width: 32, height: 32 }} />
```

```tsx
// Coloring via currentColor
<span style={{ color: "red" }}>
  <Search />
</span>
```

```tsx
// Dynamic icon rendering
import { Icon } from "@repo/vesper/icons";

<Icon kind="gear" />
<Icon kind="search" />
<Icon kind="tenstorrent" />
```
