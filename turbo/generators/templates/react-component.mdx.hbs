---
title: {{ titleCase (sentenceCase component) }}
description: Short component description
---

# {{ titleCase (sentenceCase component) }}

Long component description

<{{ pascalCase component }} />

## Options

| Prop       | Type       | Description      | Default        |
| ---------- | ---------- | ---------------- | -------------- |
| `propName` | `propType` | Prop description | `defaultValue` |

## Examples

```tsx
import { {{ pascalCase component }} } from "@tenstorrent/vesper/{{ component }}";

// Basic usage
<{{ pascalCase component }} />;
```
