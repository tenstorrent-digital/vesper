{{#if element}}
import type { ComponentProps } from 'react';
import { cn } from '@/utils/cn';

export interface {{ name }}Props extends ComponentProps<'{{ element }}'> {}

/**
 * {{ name }} component description, params, and example usage
 *
 * @param {ParamType} [props.optionalParam] - (optional) The prop description. @default value
 * @param {ParamType} props.requiredParam - The prop description
 *
 * @example
 * <{{ name }} />
 */
export function {{ name }}({ className, ...props }: {{ name }}Props) {
  return <{{ element }} className={cn("vesper-{{ kebabCase name }}", className)} {...props} />;
}
{{else}}
export interface {{ name }}Props {}

export function {{ name }}(props: {{ name }}Props) {
  return null;
}
{{/if}}
