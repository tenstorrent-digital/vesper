---
extends: existence
message: "AI mic-drop: '%s'. Integrate the point into the surrounding text or cut it."
level: error
nonword: true
tokens:
  # ==========================================================================
  # Staccato declarations: short punchy sentences AI drops in for dramatic
  # effect. "All four are thin." "It matters." "And it shows."
  #
  # These read like someone slamming a gavel in what should be a calm
  # technical discussion. Human technical writers state facts inline;
  # AI punctuates with mic-drops.
  # ==========================================================================

  # ---- Pronoun + intransitive verb mic-drops ----
  # "It matters." "This works." "That scales."
  - "\\b(?:It|This|That) matters\\."
  - "\\b(?:It|This|That) works\\."
  - "\\b(?:It|This|That) shows\\."
  - "\\b(?:It|This|That) helps\\."
  - "\\b(?:It|This|That) scales\\."
  - "\\b(?:It|This|That) compounds\\."
  - "\\b(?:It|This|That) counts\\."
  - "\\b(?:It|This|That) adds up\\."
  - "\\b(?:It|This|That) pays off\\."
  - "\\b(?:It|This|That) depends\\."

  # ---- Explicit-NP subject + intransitive mic-drop verb ----
  # Same family as the pronoun set above, but with definite or demonstrative
  # noun-phrase subjects instead of bare pronouns. AI punctuates technical
  # discussion with these terminal one-liners.
  # "The unset-versus-bogus distinction matters." "The trust compounds."
  # [\w-] catches hyphenated subjects ("load-bearing", "user-facing").
  - "\\b(?:The|This|That) [\\w-]+(?: [\\w-]+){0,2} (?:matters|compounds|pays off|adds up)\\."

  # Plural form: "These details matter." "Those choices add up."
  - "\\b(?:These|Those) [\\w-]+(?: [\\w-]+){0,2} (?:matter|compound|pay off|add up)\\."

  # ---- "This/That/It + copula + adjective" emphatic declarations ----
  # "This is key." "That's intentional." "It was deliberate."
  - "\\b(?:It|This|That)(?:'s| is| was) (?:key|crucial|critical|intentional|deliberate|powerful|elegant|subtle|clean|simple|clear|important|significant|telling|striking|notable|profound|revealing|transformative|remarkable|essential|fundamental|beautiful|paramount|vital|obvious|neat|real|enough)\\."
  - "\\b(?:It|This|That)(?:'s| is| was) (?:by design|on purpose|no accident|no coincidence|not optional)\\."

  # ---- "They/These/Those + copula + adjective" ----
  # "These are thin." "Those were deliberate."
  - "\\b(?:They|These|Those)(?:'re| are| were) (?:thin|key|crucial|critical|intentional|deliberate|powerful|elegant|subtle|clean|simple|clear|important|significant|telling|striking|notable|profound|essential|vital|obvious|neat|enough)\\."

  # ---- "The [noun] is [adjective]." short dramatic assessments ----
  # "The result is clean." "The difference is subtle."
  - "\\bThe \\w+ (?:is|are|was|were) (?:thin|clean|simple|subtle|clear|key|crucial|critical|intentional|deliberate|powerful|elegant|important|significant|telling|striking|notable|straightforward|profound|revealing|transformative|remarkable|essential|fundamental|beautiful|obvious|neat|real|paramount|vital)\\."
  - "\\bThe \\w+ \\w+ (?:is|are|was|were) (?:thin|clean|simple|subtle|clear|key|crucial|critical|intentional|deliberate|powerful|elegant|important|significant|telling|striking|notable|straightforward|profound|revealing|transformative|remarkable|essential|fundamental|beautiful|obvious|neat|real|paramount|vital)\\."

  # ---- Quantifier/determiner + copula + adjective mic-drops ----
  # Short declarations with a quantifier subject and a single-adjective verdict.

  # "All [noun] are [adjective]." — "All four are thin." "All three are identical."
  - "\\bAll \\w+ (?:are|were) (?:thin|clean|simple|subtle|clear|key|crucial|critical|intentional|deliberate|powerful|elegant|important|significant|telling|striking|notable|straightforward|profound|essential|obvious|neat|identical|interchangeable|equivalent)\\."

  # "Both [are/noun are] [adjective]." — "Both are thin." "Both layers are thin."
  - "\\bBoth (?:are|were) (?:thin|clean|simple|subtle|clear|key|crucial|critical|intentional|deliberate|powerful|elegant|important|significant|telling|notable|straightforward|essential|obvious|identical|interchangeable|equivalent)\\."
  - "\\bBoth \\w+ (?:are|were) (?:thin|clean|simple|subtle|clear|key|crucial|critical|intentional|deliberate|powerful|elegant|important|significant|telling|notable|straightforward|essential|obvious|identical|interchangeable|equivalent)\\."

  # "Each/Every [noun] is [adjective]." — "Each handler is clean." "Every choice is deliberate."
  - "\\b(?:Each|Every) \\w+ (?:is|was) (?:thin|clean|simple|subtle|clear|key|crucial|critical|intentional|deliberate|powerful|elegant|important|significant|telling|striking|notable|straightforward|profound|essential|obvious|neat|identical|independent|self-contained)\\."
  # "Each is [adjective]." — standalone without noun
  - "\\bEach (?:(?:one )?(?:is|was)) (?:thin|clean|simple|subtle|clear|key|crucial|critical|intentional|deliberate|powerful|elegant|important|significant|telling|notable|straightforward|essential|obvious|identical|independent|self-contained)\\."

  # "Everything/Nothing is [adjective]." — "Everything is intentional." "Nothing is accidental."
  - "\\bEverything (?:is|was) (?:intentional|deliberate|purposeful|connected|composable|explicit|visible|traceable|testable|optional)\\."
  - "\\bNothing (?:is|was) (?:accidental|random|hidden|implicit|optional|wasted|redundant|superfluous|arbitrary|magical|surprising)\\."

  # "None are [adjective]." — "None are optional." "None are accidental."
  - "\\bNone (?:are|were|is|was) (?:optional|accidental|random|redundant|superfluous|arbitrary|wasted|surprising|hidden|magical)\\."

  # ---- Antithetical balance: "X changes, Y stays the same." ----
  # Two balanced clauses with change/permanence verb opposition, creating
  # a pithy aphorism. AI reaches for this structure constantly.
  # "The adapter changes, the service call stays the same."
  # "Tools change, principles don't."
  # "Frameworks come and go, fundamentals remain."

  # Change-verb, then stay-verb (comma / semicolon / em-dash separated)
  - "\\b(?:changes?|evolves?|shifts?|varies|vary|comes? and go(?:es)?)[,;—] (?:the |a |an )?(?:\\w+ ){0,4}(?:stays? the same|remains?(?: the same)?|holds?|persists?|endures?|(?:doesn't|don't) change)\\."

  # Stay-verb, then change-verb (reverse order)
  - "\\b(?:stays? the same|remains?(?: the same)?|holds?|persists?|endures?)[,;—] (?:the |a |an )?(?:\\w+ ){0,4}(?:changes?|evolves?|shifts?|varies|vary|comes? and go(?:es)?)\\."

  # Period-separated: "Tools change. Principles don't."
  - "\\b(?:changes?|evolves?|shifts?|varies|vary)\\. [A-Z](?:\\w+ ){0,3}(?:stays? the same|remains?|holds?|persists?|endures?|(?:doesn't|don't) change)\\."
  - "\\b(?:stays? the same|remains?|holds?|persists?|endures?)\\. [A-Z](?:\\w+ ){0,3}(?:changes?|evolves?|shifts?|varies|vary)\\."

  # ---- Verbless fragment mic-drops ----
  # Short sentences with no main verb, used as paragraph closers.
  # Distinct from the copula patterns above — these have no verb at all.

  # Bare adjective list fragments: "Simple, predictable, testable."
  - "\\b(?:Pure|Clean|Simple|Clear|Fast|Small|Thin|Lean|Cheap|Safe|Flat|Sane|Strict|Minimal|Explicit|Composable|Predictable|Testable|Readable|Maintainable|Reproducible|Intentional|Deliberate|Declarative|Idiomatic|Immutable|Ergonomic|Deterministic|Straightforward|Opinionated), (?:\\w+, )*(?:and )?\\w+\\."

  # Parallel noun-phrase fragment pairs: "Clean separation, clear ownership."
  - "\\b(?:Clean|Clear|Pure|Simple|Single|One|Zero|Full|Same|Thin|Flat|Strict|Minimal|Explicit|Complete|Total|Direct|Separate) \\w+, (?:clean|clear|pure|simple|single|one|zero|full|same|thin|flat|strict|minimal|explicit|complete|total|direct|separate|different|no|fewer?) \\w+\\."

  # Comparative-adjective balanced fragments:
  # "Stronger isolation, more lifecycle machinery."
  # "Faster delivery, fewer surprises."
  # Comparative adjective + 1-2 word noun, paired with another
  # comparative + noun. The balanced tradeoff aphorism pattern.
  - "\\b(?:Stronger|Weaker|Faster|Slower|Better|Worse|Cleaner|Simpler|Smaller|Larger|Bigger|Higher|Lower|Tighter|Looser|Smoother|Newer|Older|Richer|Leaner|Safer|Saner|More|Less|Fewer) [\\w-]+(?: [\\w-]+)?, (?:stronger|weaker|faster|slower|better|worse|cleaner|simpler|smaller|larger|bigger|higher|lower|tighter|looser|smoother|newer|older|richer|leaner|safer|saner|more|less|fewer) [\\w-]+(?: [\\w-]+)?\\."

  # "Just/Pure/Clean + noun" standalone fragments: "Pure orchestration." "Just data."
  - "\\b(?:Just|Pure|Clean|Plain|Bare|Raw|Straight|Strict|Simple|Mere) \\w+\\."

  # "Just X, nothing more/else" closers: "Just data, nothing more."
  - "\\bJust \\w+, nothing (?:more|else|fancy|special|clever)\\."

  # Fragment + dramatic qualifier spliced with "and":
  # "Pure orchestration, and the one package that imports from all layers."
  - "\\b(?:Pure|Clean|Just|Simple|Plain|Bare|Raw|Mere|Thin|Flat) \\w+, and [^.]+\\."

  # Terse prepositional callback: "With PostgreSQL, one."
  # A telegraphic fragment that only makes sense as a parallel to the
  # preceding sentence. The single-word answer is the mic-drop.
  - "\\bWith \\w+(?:[ -]\\w+)?, \\w+\\."

  # ---- Dramatic codas: "And [short clause]." ----
  # "And it shows." "And that's okay." "And that's the point." is
  # PointVerdict's.
  - "\\bAnd it shows\\."
  - "\\bAnd it works\\."
  - "\\bAnd it matters\\."
  - "\\bAnd it helps\\."
  - "\\bAnd it scales\\."
  - "\\bAnd that'?s okay\\."
  - "\\bAnd that'?s fine\\."
  - "\\bAnd that'?s enough\\."
  - "\\bAnd that'?s it\\."

  # ---- "Full stop" / "Period" as emphasis ----
  # Standalone one-word emphatic "Period." only; anchored to a sentence
  # boundary so the ordinary noun ("billing period.", "grace period.")
  # stays clean. Capitalized form only — the lowercase noun is the FP.
  - "\\bFull stop\\."
  - "(?:^|[.!?\"] )Period\\."

  # ---- Contrastive fragment mic-drops ----
  # "X, not Y." — contrastive fragment with no verb
  # "Dense, not cramped." "Fast, not sloppy." "Opinionated, not rigid."
  - "\\b[A-Z][a-z]+, not [a-z]+\\."
  - "\\b[A-Z][a-z]+, never [a-z]+\\."

  # Bare "Not X." / "No X." standalone fragment: "Not now."
  # "No singleton." AI deploys these as one-word verdicts — a tiny
  # period-bound answer to a question the surrounding prose never
  # asked. The broader regex catches both negation forms
  # with any single-word tail. Some conversational fragments
  # ("No idea.", "No way.", "Not bad.") fire too; acceptable for
  # an error-level rule in technical-doc contexts.
  - "\\bNot? \\w+\\."

  # Verb-led contrastive: the extended-sentence remediation cheat
  # that appears after the bare-fragment form above gets flagged.
  # "Isolation here stays logical, not physical."
  # "The output reads clean, not cluttered."
  # "Code becomes declarative, not imperative."
  # Subject is 1-3 words; the linking-ish verb list mirrors the
  # copula sense ("X stays/remains/becomes Y" = "X is Y").
  - "\\b[A-Z][a-z]+(?: \\w+){0,2} (?:stays?|remains?|becomes?|feels?|reads?|seems?|sounds?|looks?|appears?|grows?|ends? up|comes? across) [a-z-]+, not [a-z-]+\\."
  - "\\b[A-Z][a-z]+(?: \\w+){0,2} (?:stays?|remains?|becomes?|feels?|reads?|seems?|sounds?|looks?|appears?|grows?|ends? up|comes? across) [a-z-]+, never [a-z-]+\\."

  # Verb-led contrastive with non-linking verbs + noun objects:
  # "They share a contract, not code."
  # "Custom signals belong in stderr text, not in the exit number."
  # Different construction from the linking-verb family above — the object
  # is a noun phrase rather than an adjective.
  - "\\b[A-Z][a-z]+(?: \\w+){0,2} (?:shares?|keeps?|holds?|lives?|belongs?|sits?|points?|goes|go|gives?|takes?) (?:(?:in|on|to|at|with|for) )?[\\w-]+(?: [\\w-]+){0,3}, not (?:(?:in|on|to|at|with|for) )?[\\w-]+(?: [\\w-]+){0,3}[.:]"

  # "X over Y." — preference fragment
  # "Clarity over cleverness." "Depth over breadth."
  - "\\b[A-Z][a-z]+ over [a-z]+\\."

  # Comparative aphorism: "X verbs comparative-adjective than Y."
  # "Disk costs less than lost context."
  # "Tests run faster than docs."
  # Concise, sentence-final, AI tutorial-blog tone.
  - "\\b[A-Z][a-z]+(?: \\w+){0,2} \\w+s (?:more|less|fewer|faster|slower|cheaper|cleaner|simpler|safer|tighter|better|worse|larger|smaller|bigger|higher|lower|longer|shorter) than [^.!?]+\\."

  # "X, not Y, not Z." — extended contrastive
  - "\\b[A-Z][a-z]+, not [a-z]+, not [a-z]+\\."

  # "X, then Y." — sequencing fragment
  # "Scannable, then readable." "Simple, then fast."
  - "\\b[A-Z][a-z]+, then [a-z]+\\."

  # "X first, Y second." — priority fragment
  - "\\b[A-Z][a-z]+ first, [a-z]+ second\\."
  - "\\b[A-Z][a-z]+ first, [a-z]+ never\\."

  # "Verb in the noun." — imperative mic-drop
  # "Invest in the system." "Trust the process." "Ship the thing."
  - "\\b(?:Invest|Trust|Embrace|Ship|Build|Start|Keep|Focus|Lean|Bet|Play|Own|Do) (?:in |on |into )?the [a-z]+\\."

  # "[Noun] is a [noun]." — categorical declaration as closer
  # "Density is a feature." "Complexity is a choice."
  - "\\b[A-Z][a-z]+ is an? [a-z]+\\."

  # "the X, not the Y" — tagline gloss after a colon
  # "Implicit positioning: the reference shelf, not the opinion column."
  - ": the [^,]+, not the [^.]+"
