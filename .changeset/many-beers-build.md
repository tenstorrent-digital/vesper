---
"@tenstorrent/vesper": minor
---

Adjust components that use radix portals to prioritize nearest dialog as portal target. Add a `container` prop to `Tooltip`, `Menu`, and `Select` to override the portal target.
