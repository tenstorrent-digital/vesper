---
"@tenstorrent/vesper": minor
---

Update components with text inputs to always render at their `"md"` size variant on mobile screens for a11y purposes. This change affects the `select`, `combobox`, `text-input`, `masked-input`, and `text-area` components.
